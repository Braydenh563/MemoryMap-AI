# INBOX 426: the UI agent, 2026-09-25 and 26

Port 8793, data dir /tmp/mm-ui-8793. Worktree cut from fix/gemini-fixes-5,
merged up to 9825ee4. Round 1 was 426 a, b and h; round 2 was round 1's
own leftovers, uipolish-0924's, the "not verified" list and a sweep pass;
round 3 was the second batch's u, y and z.

## Landed

Round 1 (merged): 426 h, the logo under reduced motion; 426 a, the
formatting strip folds behind More and focus mode has Tools; 426 b, the
suggestions panel is a size container and opens from focus mode.

Round 2:
- The strip refits when its contents change (a MutationObserver reduced
  to a layout signature); below 600px of strip the layout toggle folds
  behind More first; `--doc-prose-w` lives on `#tab-documents`, so focus
  mode's side panel keeps the grip's width.
- Settings: Skills, Templates and Personas rows 78 -> 66px.
- The graph node sheet meets the touch floor; `#wb-context` on a phone
  moves to the canvas foot when the top band would cover the selection
  (0px2 over the selection on all ten of `wbcontextphone.js`).
- Checked: flat looks, glass off, touch, the About and welcome logos. The
  strip's own group is square at the touch floor.
- Sweeps at 1440, 1024, 820 and 390: the Notes capture strip before
  documents.js loads, tablet toasts off the sub-tabs, light `--warn` and
  `--error` one step darker.

Round 3:
- (u) After a pointer pick, Settings' pane and a tab's page take the
  focus, so the reading keys scroll it instead of walking the nav (the
  "jumping between sections"); Settings blurs nothing behind its
  scrolling pane (83-100ms frames with glass -> 16.7); fields end at the
  row's right edge; the empty Profile card is gone.
- (z) Library Activity is one column, in words, with its underscores; the
  Bin's tick keeps a gap from the ⋯; the suggestions head is one icon
  group when narrow; "Fill the whole screen" asks the desktop window
  (`core/window_hook.py`, `/desktop/fullscreen`); the guide's Atlas is a
  disc the height of its title.
- (y) The tour follows its control every frame (`tourWatchFrame`); a
  settings step opens the fold its control is in; the maps section says
  when there is no map.

## Left, in order

Round 4 closed the first three (the text-safe `--accent-text` token and
its lint, the graph empty state's emblem slot, the expanded strip's group
at 1024) and planned the app.js split in `appjs-split.md`.

1. The app.js split, when the orchestrator says go: `appjs-split.md`,
   step 0 first.
2. The dashboard jump (u) was not reproduced by wheel, idle (60s, no
   layout shift) or any scroll call (`scrollwatch.js`); the keyboard half
   is fixed. If it recurs, run `scrolljump.js` with the owner's
   preferences (`LS=`) and the companion on.
3. uipolish-0924 items still open: `deadbtn.js`'s Chat export and delete
   findings, the icon-only touch floor app-wide (dialogs and the strip
   have it now), the surface-by-surface pass (its item D).

## Not verified

- The desktop window: the full-screen toggle is driven through a stand-in
  in tests; pywebview's `toggle_fullscreen` on the owner's WebView2 was
  not run. The frame-time numbers are headless Chromium's software
  compositor, where a blur costs more than on a GPU; the ratio (5x) is
  the finding, not the milliseconds.
- Images 87 and 95 were not reproduced (the tour at 1184 to 1600, basics
  into Notes and Settings, covered nothing in any 50ms sample); the fix
  is the per-frame follow, proven against a forced layout shift.
- Dark theme for rounds 2 and 3 was measured by contrast.js at 1440 only
  (35 of 35 surfaces ok); the light sweeps ran at all four widths.
- Full suite not run (targeted tests and the lint set per step).
