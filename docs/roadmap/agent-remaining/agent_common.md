## Rules every agent carries (read CLAUDE.md first; these are the traps)
- You are in your OWN git worktree on your own branch cut from `claude/epic-ramanujan-8xocc0`. Commit there with plain `git add <your paths>`; never `git add -A`. One commit per step. The orchestrator merges. Do NOT push.
- Commit trailers on every commit, verbatim:
  `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` and `Claude-Session: https://claude.ai/code/session_01TDrFtNMQzyhymKfCS5reYn`. No model identifiers anywhere else.
- Before each commit: `bash scripts/gate.sh --staged` (lints, node --check, ruff) plus the targeted tests for files you touched. Never run the full suite.
- Run the app from your worktree: `bash scratchpad/ui-sweeps/serve.sh <PORT> <DATADIR>` (both given below; never another port). Sweeps: `BASE=http://127.0.0.1:<PORT> PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers timeout 110 node scratchpad/ui-sweeps/<x>.js`. `lib.js`'s `boot(opts)` RETURNS `{browser, ctx, page, OUT}`; it is not a callback API. Password `testpassword123`; `THEME=dark` for dark. Login: fill `#lock-password`, click `#lock-submit`. Never `waitUntil: "networkidle"`. Restart the server after any Python change. Never `pkill -f uvicorn`: use `pgrep -f '[u]vicorn.*--port <PORT>' | xargs -r kill`, and never on the same shell line as a start.
- Measure, do not look: `getComputedStyle`, `getBoundingClientRect`, `scrollHeight` vs `clientHeight`, `scratchpad/pngpixel.py`. Every claim in your report is a number from a sweep you name. Reproduce a bug before theorising; say plainly when you could not.
- Copy: sentence case, no em-dashes (a lint fails), no exclamation marks, no "Oops". Inline `style=` is refused by the CSP: `el.style.x =` or a class. New UI from DESIGN.md's recipe index (`kebabMenu`, `.dock`, `data-help-for`, tokens; inner radius `var(--radius-inner)`); a need the index lacks gets its recipe row and lint in the same commit.
- Do not install torch or sentence-transformers. Do not create new plan documents. Decisions in a plan's "Decisions made" are not remade.
- Docs hygiene per commit: CHANGELOG.md line (and docs/CHANGELOG.md, same text); an INBOX item you fix gets `**Fixed <hash>.**` plus a measured line under it, then `python scratchpad/inbox_resolve.py <n>` moves it to HISTORY; a plan step you finish has its Built block moved to HISTORY.md ("Moved from the plans") leaving a one-line pointer. `tests/test_plan_hygiene.py`, `test_docs_layout.py`, `test_readme_freshness.py` must pass.
- Before stopping: update your `docs/roadmap/agent-remaining/<file>.md` (what landed with hashes, what is left with file, id and next step, found-not-fixed, not verified), commit it, and report five lines: status, commits, numbers, not verified, found-not-fixed. Never a transcript.
- Budget: work through the whole list in order; do not stop after one item. If you hit a rate limit, commit what is clean first.
- TOKEN RULE (the owner, 2026-09-13): minimum tokens. No narration, no restating the brief, no plans in prose; think silently. Read only the line ranges you need (`sed -n`, `grep -n`), never whole files. Commit bodies: three lines at most. Reports: five lines, nothing else. Do not re-read files you edited. Skip screenshots; numbers only.
- SAVE RULE (the owner, 2026-09-13): commit after every item, and a `WIP: <item>` commit at any point you have been editing for more than ten minutes without one (squash it into the item's commit when the item lands). After each commit, append two lines to your `docs/roadmap/agent-remaining/<file>.md` (done: hash; next: item, file, line) and include that file in the commit. A run cut off at any moment must be resumable from `git log` plus that file alone.
- PUSH RULE (the owner, 2026-09-13): after EVERY commit run `git push -q -f origin HEAD:agent/wip-<yourname>` (your name is given in your brief). Never push to any other branch.
