# INBOX 426 a, b, h: the UI agent, 2026-09-25

Port 8793, data dir /tmp/mm-ui-8793. Worktree cut from fix/gemini-fixes-5.

## Landed

- 426 h, the logo. The reduced-motion blanket in 02-chat-graph.css had
  gained `#nm-buddy` inside its `:not(:is(...))` exemption, which raised
  it to specificity 1,0,0 and let it beat the emblem's `:root ...
  !important` restatement: every emblem turned once in 0.01ms and stopped
  whenever the system asked for reduced motion. The exemption is inside
  `:where()` now. The chat welcome's short shape (1280x720) kept the
  emblem at 32/52 instead of hiding it; the getting-started card and the
  chat reply avatar turn too. `tests/test_emblem_turns.py`.
- 426 a, the formatting strip. One-row mode no longer scrolls: it folds
  what does not fit behind More (`fitDocToolbarRow`, documents.js), and
  the end group is no longer a sticky patch over Insert. The 720px band's
  hidden scroller is gone the same way. Focus mode's floating dock gains
  Tools (the dock and the strip back, remembered with the mode). DESIGN.md
  recipe row. `tests/test_doc_toolbar_reach.py`,
  `scratchpad/ui-sweeps/doctoolbarfit.js`.
- 426 b, the suggestions panel. A size container (`@container
  doc-prose`): the head wraps, its buttons stay whole, both halves of a
  finding carry a title. Focus mode's dock gains Suggestions, which opens
  the panel as a side panel the page makes room for.
  `tests/test_doc_prose_panel_fit.py`.

## Left, in order

1. The uipolish-0924 leftovers were not reached: the graph node panel,
   `#wb-context`, sheets at 390, toasts, the `deadbtn.js` findings, the
   icon-only touch floor, and Settings' Skills rows at about 78px each.
   Unchanged in `agent-remaining/uipolish-0924.md`.
2. At 360 the one-row strip shows only two tools beside its own four
   (More, layout, line numbers, collapse): the end group costs 176px of a
   300px strip. Worth deciding whether the layout toggle belongs in the
   strip at all below 600, since the phone bar is the phone's toolbar.
3. `fitDocToolbarRow` refits on a width change, a mode change and a
   collapse. A strip whose contents change at a constant width (a control
   shown or hidden by file type) is not refitted until the next resize;
   no such control was found in the strip today, but a MutationObserver
   would close it for good.
4. In focus mode the side panel is 20rem whatever the grip set on the
   page, because the page's padding has to know the width and the grip's
   property lives on the panel. Moving `--doc-prose-w` onto
   `#tab-documents` would let both read the grip's value.

## Not verified

- Dark theme, the flat looks and the glass-off setting for all three
  changes: every measurement here was light theme, Classic look.
- The owner's desktop window (WebView2 with Windows' animation effects
  off) was not driven; the reduced-motion cause was reproduced in headless
  Chromium with `reducedMotion: 'reduce'`, which is the same media query.
- The About, onboarding and graph-empty emblems were not measured on
  screen (hidden in the sweep); they use the same class and rule.
- Touch: More, Tools and Suggestions under a coarse pointer, and the
  on-screen keyboard with the strip folded at 360.
- Full suite not run (targeted tests and the lint set only).

## Found, not fixed

- The main checkout's `.venv` has torch installed (438MB `libtorch_cpu.so`),
  against CLAUDE.md section 7, and `tests/test_launcher_scripts.py` copies
  that venv into two scratch repos per run. With several agents running
  the suite the disk filled (0 bytes free on /) mid-session; removing this
  agent's own pytest basetemp freed 3.5G. Older basetemps from other runs
  (`/tmp/f2-pt`, `/tmp/p2-pt`, `/tmp/pytest-of-root/pytest-13xx`) each hold
  about 4.7G of the same copies.
